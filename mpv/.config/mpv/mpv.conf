
# TEST #

# idle=yes       # keep mpv running with no file playing
# pause=yes      # start paused if something *is* loaded

## Audio Output
# ao=pipewire

# Video
# hwdec=auto # FROM NixOS Docs
# hwdec=yes
# hwdec=vaapi,nvdec,auto
# vo=gpu

# Hardware acceleration settings for Intel HD 4600
# hwdec=vaapi
hwdec=vaapi,nvdec,auto
# hwdec-codecs=all
vo=gpu # FROM NixOS Docs

# Force Intel GPU device
# gpu-api=opengl
gpu-context=wayland # Change to "x11" if not using Wayland

# VAAPI specific settings
# vaapi-device=/dev/dri/renderD128 # Intel integrated GPU

# Quality and performance
# profile=gpu-hq
# scale=ewa_lanczossharp
# cscale=ewa_lanczossharp

# vo=vaapi
# vo=dmabuf-wayland #black screen for h265
##The default profile which sets some recommended settings
#it makes youtube videos slow!
# profile=gpu-hq # FROM NixOS Docs
# gpu-context=wayland # On wayland only FROM NixOS Docs

# gpu-context=wayland
# stop-screensaver=yes
# gpu-backend=wayland

##The called API
##Use "opengl" if you have compatibility issues.For displaying HDR content (not HDR -> SDR) "d3d11" is recommended (Windows only). 
#gpu-api=vulkan

# Player #
#input-ipc-server=/tmp/mpvsocket
# hr-seek-framedrop=no
#no-resume-playback
# border=no
# msg-color=yes
# msg-module=yes

# Screenshots #           
# Video DIR         
#screenshot-template="%x/Screens/Screenshot-%F-T%wH.%wM.%wS.%wT-F%{estimated-frame-number}"
# HOME DIR
screenshot-template="~/Pictures/Pictures/SnapShots/mpv-%F-T%wH.%wM.%wS.%wT-F%{estimated-frame-number}"
screenshot-format=png					# Set screenshot format
screenshot-png-compression=0			# Range is 0 to 10. 0 being no compression.
screenshot-tag-colorspace=yes
screenshot-high-bit-depth=yes			# Same output bitdepth as the video

#OSC/OSD #

# osc=yes
osc=no
osd-bar=no # uosc script provides seeking & volume indicators (via flash-timeline and flash-volume commands)
# osd-bar=yes # Do not remove/comment if mpv_thumbnail_script_client_osc.lua is being used.
osd-font='Source Sans Pro'				# Set a font for OSC
osd-font-size=34						# Set a font size
osd-color='#CCFFFFFF'                   # ARGB format
# osd-border-color='#DD322640'            # ARGB format
osd-bar-align-y=-1                      # progress bar y alignment (-1 top, 0 centered, 1 bottom)
osd-border-size=2                       # size for osd text and progress bar
osd-bar-h=3                             # height of osd bar as a fractional percentage of your screen height
osd-bar-w=65                            # width of " " "
#deadzonesize=1 #Failed

# catppuccin-macchiato-mauve

## Main mpv options
#background-color='#24273a'
osd-back-color='#181926'
osd-border-color='#181926'
## osd-color='#cad3f5'
osd-shadow-color='#24273a'

## Stats script options
## Options are on separate lines for clarity
## Colors are in #BBGGRR format
script-opts-append=stats-border_color=30201e
script-opts-append=stats-font_color=f5d3ca
script-opts-append=stats-plot_bg_border_color=f6a0c6
script-opts-append=stats-plot_bg_color=30201e
script-opts-append=stats-plot_color=f6a0c6

## External script options
## It is fine to leave these here even if one does not use these scripts because they are just ignored unless a script uses them

## UOSC options
script-opts-append=uosc-color=foreground=c6a0f6,foreground_text=363a4f,background=24273a,background_text=cad3f5,curtain=1e2030,success=a6da95,error=ed8796

# yt-dlp
ytdl=yes
# ytdl-format=bv[vcodec!~='vp0?9']+ba/b
# ytdl-raw-options=cookies-from-browser=firefox
# ytdl-format="bestvideo[height<=720]+bestaudio/best"
# ytdl-format=bv[height<=1080][vcodec!~='^(vp0?9)']+ba/b[height<=1080]
ytdl-format=bv[height<=720][vcodec~='^(vp0?9)']+ba/b[height<=720]


# Mine #

# Subtitle

# sub-codepage=utf8:cp1256
# sub-text-font=Bahij Droid Naskh
# sub-font=Dubai
sub-font="Osama Font"
# sub-font="Droid Arabic Naskh"
# sub-text-font=Calibri
# sub-pos=90
sub-margin-y=110
sub-font-size=70
sub-border-size=2
# sub-border-color='#282828'
sub-border-color='#101010'
# sub-color=1.0/0.65
# sub-color='#C0808080'
sub-color=1.0/1.0/1.0/0.85
sub-blur=.3
# stretch-dvd-subs=yes
stretch-image-subs-to-screen=yes
# image-subs-video-resolution=yes
slang=ar,ara,arabic,en,eng,english
sub-visibility=no
track-auto-selection=yes
subs-with-matching-audio=no

# Audio

#audio-channels=stereo
#ad-lavc-downmix=yes
#af=lavfi="pan=stereo|FL < 0.5*FC + 0.3*FLC + 0.3*FL + 0.3*BL + 0.3*SL + 0.5*LFE | FR < 0.5*FC + 0.3*FRC + 0.3*FR + 0.3*BR + 0.3*SR + 0.5*LFE",lavfi="acompressor=10"
volume-max=200
# volume=40
# alang=ar,ara,arabic,en,eng,english
alang=en,eng,english,ar,ara,arabic

# audio-files-add="2"
# audio-files-add="3"
# audio-files-add="4"

# Window

# autofit=900x900
#brightness=2
#gamma=1
save-position-on-quit=yes
cache=auto
keep-open=yes
keep-open-pause=yes

## Profiles ##

# [downmix]
# audio-channels=stereo
# ad-lavc-downmix=yes
# af=lavfi="pan=stereo|FL < 0.5*FC + 0.3*FLC + 0.3*FL + 0.3*BL + 0.3*SL + 0.5*LFE | FR < 0.5*FC + 0.3*FRC + 0.3*FR + 0.3*BR + 0.3*SR + 0.5*LFE",lavfi="acompressor=10"

# [downmix]
# af=lavfi="pan=stereo|FL < 0.5*FC + 0.3*FLC + 0.3*FL + 0.3*BL + 0.3*SL + 0.5*LFE | FR < 0.5*FC + 0.3*FRC + 0.3*FR + 0.3*BR + 0.3*SR + 0.5*LFE",lavfi="acompressor=10"
# audio-channels=stereo
# ad-lavc-downmix=yes
# profile-restore=copy-equal
# profile-cond=p["audio-params/channel-count"] > 2

#[stereo]
#af=lavfi="acompressor=10"
#profile-restore=copy-equal


#
# Example mpv configuration file
#
# Warning:
#
# The commented example options usually do _not_ set the default values. Call
# mpv with --list-options to see the default values for most options. There is
# no builtin or example mpv.conf with all the defaults.
#
#
# Configuration files are read system-wide from /usr/local/etc/mpv.conf
# and per-user from ~/.config/mpv/mpv.conf, where per-user settings override
# system-wide settings, all of which are overridden by the command line.
#
# Configuration file settings and the command line options use the same
# underlying mechanisms. Most options can be put into the configuration file
# by dropping the preceding '--'. See the man page for a complete list of
# options.
#
# Lines starting with '#' are comments and are ignored.
#
# See the CONFIGURATION FILES section in the man page
# for a detailed description of the syntax.
#
# Profiles should be placed at the bottom of the configuration file to ensure
# that settings wanted as defaults are not restricted to specific profiles.

##################
# video settings #
##################

# Start in fullscreen mode by default.
#fs=yes

# force starting with centered window
#geometry=50%:50%

# don't allow a new window to have a size larger than 90% of the screen size
#autofit-larger=90%x90%

# Do not close the window on exit.
#keep-open=yes

# Do not wait with showing the video window until it has loaded. (This will
# resize the window once video is loaded. Also always shows a window with
# audio.)
#force-window=immediate

# Disable the On Screen Controller (OSC).
#osc=no

# Keep the player window on top of all other windows.
#ontop=yes

# Specify high quality video rendering preset (for --vo=gpu only)
# Can cause performance problems with some drivers and GPUs.
#profile=gpu-hq

# Force video to lock on the display's refresh rate, and change video and audio
# speed to some degree to ensure synchronous playback - can cause problems
# with some drivers and desktop environments.
#video-sync=display-resample

# Enable hardware decoding if available. Often, this does not work with all
# video outputs, but should work well with default settings on most systems.
# If performance or energy usage is an issue, forcing the vdpau or vaapi VOs
# may or may not help.
#hwdec=auto

##################
# audio settings #
##################

# Specify default audio device. You can list devices with: --audio-device=help
# The option takes the device string (the stuff between the '...').
#audio-device=alsa/default

# Do not filter audio to keep pitch when changing playback speed.
#audio-pitch-correction=no

# Output 5.1 audio natively, and upmix/downmix audio with a different format.
#audio-channels=5.1
# Disable any automatic remix, _if_ the audio output accepts the audio format.
# of the currently played file. See caveats mentioned in the manpage.
# (The default is "auto-safe", see manpage.)
#audio-channels=auto

##################
# other settings #
##################

# Pretend to be a web browser. Might fix playback with some streaming sites,
# but also will break with shoutcast streams.
#user-agent="Mozilla/5.0"

# cache settings
#
# Use 150MB input cache by default. The cache is enabled for network streams only.
#cache-default=153600
#
# Use 150MB input cache for everything, even local files.
#cache=153600
#
# Disable the behavior that the player will pause if the cache goes below a
# certain fill size.
#cache-pause=no
#
# Read ahead about 5 seconds of audio and video packets.
#demuxer-readahead-secs=5.0
#
# Raise readahead from demuxer-readahead-secs to this value if a cache is active.
#cache-secs=50.0

# Display English subtitles if available.
#slang=en

# Play Finnish audio if available, fall back to English otherwise.
#alang=fi,en

# Change subtitle encoding. For Arabic subtitles use 'cp1256'.
# If the file seems to be valid UTF-8, prefer UTF-8.
# (You can add '+' in front of the codepage to force it.)
#sub-codepage=cp1256

# You can also include other configuration files.
#include=/path/to/the/file/you/want/to/include

############
# Profiles #
############

# The options declared as part of profiles override global default settings,
# but only take effect when the profile is active.

# The following profile can be enabled on the command line with: --profile=eye-cancer

#[eye-cancer]
#sharpen=5
